# test with validation mode
# test for existence of runtime files
# test that application is being called
# test that application is being run
# test that mocked services are being reached
# test that files are being written to remote locations in validation mode
